% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compat_aliases.R, R/fit_hybridPLS.R
\name{fit.hybridPLS}
\alias{fit.hybridPLS}
\alias{fit_hybridPLS}
\alias{print.hybridPLS}
\title{Fit Hybrid Penalized PLS}
\usage{
fit.hybridPLS(W, y, n_iter, lambda, validation_data = NULL)

fit_hybridPLS(W, y, n_iter, lambda, validation_data = NULL)

\method{print}{hybridPLS}(x, ...)
}
\arguments{
\item{W}{A \code{predictor_hybrid} object (training predictors).}

\item{y}{Numeric vector of training responses (length \code{W$n_sample}).}

\item{n_iter}{Integer number of PLS components to extract (>= 1).}

\item{lambda}{Numeric vector of smoothing parameters, one per functional
predictor (\code{length(lambda) == W$n_functional}).}

\item{validation_data}{Optional list with elements \code{W_test}
(\code{predictor_hybrid}) and \code{y_test} (numeric vector).}

\item{x}{A \code{hybridPLS} object.}

\item{...}{Unused.}
}
\value{
An object of class \code{hybridPLS}, a list with:
\describe{
\item{rho}{List of score vectors (length \code{n_iter}).}
\item{xi}{List of weight directions as single-sample \code{predictor_hybrid} objects.}
\item{beta}{List of cumulative regression directions; \code{beta[[L]]} uses the
first \code{L} components.}
\item{W}{List of deflated predictors; \code{W[[1]]} is the input, \code{W[[l+1]]}
is after extracting component \code{l}.}
\item{delta}{List of predictor loadings.}
\item{nu}{List of response loadings (scalars).}
\item{n_iter, lambda}{Fitting settings.}
\item{validation_rmse}{Numeric vector of validation RMSE by component, or \code{NULL}.}
}
}
\description{
Runs the iterative Hybrid Penalized Partial Least Squares algorithm.
If \code{validation_data} is provided, RMSE on the validation set is stored
for each extracted component.
}
\examples{
set.seed(1)
sim <- simulate_hybrid_data(n = 40, n_functional = 1, n_scalar = 2, n_basis = 5)
fit <- fit_hybridPLS(sim$W, sim$y, n_iter = 2, lambda = 1e-3)
fit
pred <- predict(fit, sim$W, n_components = 2)
cor(pred, sim$y)

}
